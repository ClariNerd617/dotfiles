#! /usr/bin/bash
# DO THIS BEFORE YOU CHANGE YOUR .bashrc, as my .bashrc will force tmux to open by default
# Install tmux plugin manager
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# To install the tmux plugins, open tmux and press `prefix` + I (capital i, as in Install) to fetch the plugins.
